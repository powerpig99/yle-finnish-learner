{
  "devDependencies": {
    "@types/chrome": "0.1.31",
    "fake-indexeddb": "6.2.5",
    "typescript": "5.9.3"
  },
  "scripts": {
    "test": "node --test tests/unit/database.test.js",
    "test:watch": "node --test --watch tests/unit/database.test.js",
    "type-check": "tsc --noEmit",
    "type-check:watch": "tsc --noEmit --watch",
    "build:extension": "tsc -p tsconfig.build.json",
    "build:extension:watch": "tsc -p tsconfig.build.json --watch",
    "validate": "npm run type-check && npm test"
  }
}
